package javax.xml.stream;

import javax.xml.stream.events.XMLEvent;

/* loaded from: rt.jar:javax/xml/stream/EventFilter.class */
public interface EventFilter {
    boolean accept(XMLEvent xMLEvent);
}
